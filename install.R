# install.R
# Packages to install when Binder / repo2docker builds the environment
install.packages('exact2x2', repos = 'https://cloud.r-project.org')
install.packages("exact2x2", repos = 'https://cloud.r-project.org')
install.packages("ggplot2", repos = 'https://cloud.r-project.org')

# Add any other packages your book uses